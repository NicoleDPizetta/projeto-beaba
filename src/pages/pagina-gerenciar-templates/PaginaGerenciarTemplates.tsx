import React from "react";
import { GridBase, Header } from "../../shared/components";
import { LayoutBase } from "../../shared/layouts";

export const PaginaGerenciarTemplates: React.FC = () => {
    return (
        <LayoutBase header={<Header />}>
        <GridBase>
            
        </GridBase>
    </LayoutBase>
    )
}