import React from "react";
import {
  Avatar,
  Typography,
  Box,
  Paper,
  IconButton,
  useTheme,
} from "@mui/material";
import MoreHorizIcon from "@mui/icons-material/MoreHoriz";

export const CardUsuario: React.FC = () => {
  const theme = useTheme();

  return (
    <Paper elevation={1} sx={{ width: "46rem" }}>
      
    </Paper>
  );
};
